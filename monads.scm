;; Bring into scope all of the monads
;; TODO: examples
;; TODO: monad transformers
;; TODO: comonad transformers

;; monads
(load "reader.scm")
(load "writer.scm")
(load "state.scm")
(load "list.scm")
(load "maybe.scm")
(load "church-maybe.scm")
(load "trivial.scm")
;; (load "cont.scm")
;; (load "free.scm")
;; (load "random.scm")
;; codensity-state

;; comonads
(load "product.scm")
;; (load "context.scm")
;; (load "exp.scm")


;; both
(load "identity.scm")
(load "yoneda.scm")
(load "cofree.scm")
;; (load "coyoneda.scm")
;; (load "density.scm")
;; (load "codensity.scm")
